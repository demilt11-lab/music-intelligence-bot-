tracking_uri: ${oc.env:MLFLOW_TRACKING_URI,http://localhost:5000}
experiment_name: ${project.name}
model_name: viral-music-predictor
artifact_location: null  # use default

auto_log: true
log_system_metrics: true

registry:
  promotion_thresholds:
    staging:
      auroc: 0.75
    production:
      auroc: 0.85
      precision: 0.70
  tags:
    team: ml
    domain: music
