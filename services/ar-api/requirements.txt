# FastAPI >= 0.139 pulls a Host-header-validating Starlette; the old 0.110
# floor capped starlette below the pin below.
fastapi>=0.139.0
# Starlette < 1.0.1 is vulnerable to the "BadHost" host-header validation
# bypass / auth bypass (CVE-2026-48710); 1.3.1 is the current patched release.
starlette>=1.3.1
uvicorn[standard]>=0.29
sqlalchemy>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4
xgboost>=2.0
pydantic>=2.6
python-dotenv>=1.0
